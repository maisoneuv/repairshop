FROM node:20-alpine AS build
WORKDIR /app
COPY frontend/package*.json ./
RUN npm ci
COPY frontend ./
RUN npm run build

# Static web server (served by Nginx in our stack)
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
RUN chmod -R go=rX /usr/share/nginx/html

