# Generated by Django 5.0.10 on 2026-03-12 18:38

from django.db import migrations, models


def backfill_task_reference_ids(apps, _schema_editor):
    Task = apps.get_model('tasks', 'Task')
    counters = {}
    for task in Task.objects.filter(reference_id__isnull=True).order_by('tenant_id', 'created_date', 'pk'):
        tenant_id = task.tenant_id
        counters[tenant_id] = counters.get(tenant_id, 0) + 1
        task.reference_id = f"T-{counters[tenant_id]}"
        task.save(update_fields=['reference_id'])


class Migration(migrations.Migration):

    dependencies = [
        ('service', '0006_cashregister_cashtransaction'),
        ('tasks', '0040_workitem_payment_register'),
        ('tenants', '0001_initial'),
    ]

    operations = [
        migrations.AddField(
            model_name='task',
            name='reference_id',
            field=models.CharField(blank=True, max_length=50, null=True),
        ),
        migrations.RunPython(backfill_task_reference_ids, migrations.RunPython.noop),
        migrations.AddConstraint(
            model_name='task',
            constraint=models.UniqueConstraint(fields=('tenant', 'reference_id'), name='unique_task_reference_per_tenant'),
        ),
    ]
